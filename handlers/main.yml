---
- name: restart prometheus
  become: true
  systemd:
    daemon_reload: true
    name: prometheus
    state: restarted
  ignore_errors: "{{ ansible_check_mode }}"

- name: reload prometheus
  become: true
  systemd:
    name: prometheus
    state: reloaded
  ignore_errors: "{{ ansible_check_mode }}"
